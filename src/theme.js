const colors = {
  black: "#222",
  white: "#fff",
};

const space = {
  xs: "4px",
  sm: "8px",
  md: "16px",
  lg: "24px",
  xl: "32px",
};

const tokens = {
  colors,
  space,
  // fontSizes,
  // fontWeights,
  // radii,
  // sizes,
  // fonts,
  // lineHeights,
  // borders,
  // shadows,
  // zIndices,
  // breakpoints,
};

export const theme = {
  ...tokens,
};
